use jquery toArray() to make an array with the id of every div

  e.g. $( "div" ).toArray()

create a function that houses forEach loop to loop through every div.

on every loop check if there's been a collision between element one and each element in the array.


collision(function(mainElement, arrayElement)){

if ($elOffset.left < $el_2_Offset.left + $el_2.width() &&
    $elOffset.left + $el.width() > $el_2_Offset.left &&
    $elOffset.top < $el_2_Offset.top + $el_2.height() &&
    $el.height() + $elOffset.top > $el_2_Offset.top) {

      $el.remove()
    }

    });


    function animateAliensSpeed(){
      if($gridArray.length < 10){
        setInterval(animateAliens, 10);
      } else {
        setInterval(animateAliens, 100);
      }
    }
    animateAliensSpeed();

    
create a setInterval function and call the loop function every second

// make the main object shoot

// make collision detection between shooting and aliens

// hide aliens upon collision using jquery

// increase score if aliens collide with main object

// game over if aliens collide with main object

// make the aliens shoot down

// // make collision detection between shooting and main object

// .remove will remove each alien from the dom

// every time the interval runs check the collision

// function that detects collision on every
